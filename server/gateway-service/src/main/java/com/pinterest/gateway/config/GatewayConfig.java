package com.pinterest.gateway.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class GatewayConfig {
        // Routes are now defined in application.yml instead of programmatically
        // This avoids conflicts and makes configuration easier to manage
}
